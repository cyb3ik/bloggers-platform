- в тестах переписать проверки ошибок на что-то подобное:
expect(res.body.errorsMessages).toEqual(
            expect.arrayContaining([
                {
                    message: expect.any(String),
                    field: 'login'
                },
                {
                    message: expect.any(String),
                    field: 'email'
                },
                {
                    message: expect.any(String),
                    field: 'password'
                }
            ])
        )
- написать тесты на пагинацию
- написать юнит тесты, в первую очередь проверить обработчик ошибок
- для лайков отдельные тесты написать -
GET bloggers-platform/api/:blogId/posts

GET bloggers-platform/api/posts
GET bloggers-platform/api/:postId
PUT bloggers-platform/api/:postId/like-status
GET bloggers-platform/api/:postId/comments

GET bloggers-platform/api/comments/:id
GET bloggers-platform/api/:commentId/like-status